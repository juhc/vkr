# Критические ошибки безопасности при настройке ОС

## Введение

Данный документ описывает наиболее распространенные и критические ошибки настройки безопасности операционных систем, которые администраторы допускают при развертывании и администрировании ОС. Эти ошибки часто становятся причиной компрометации систем и утечек данных.

**⚠️ ВНИМАНИЕ**: Данный документ создан для образовательных целей. Все описанные уязвимости используются в учебных сценариях проекта для обучения навыкам защиты операционных систем.

**Фокус документа**: Только уязвимости операционных систем (аутентификация, авторизация, управление обновлениями, учетные записи, файловая система, встроенные уязвимости ОС). Не рассматриваются уязвимости приложений, сетевых протоколов и веб-приложений.

---

## Категории ошибок ОС

1. [Ошибки в управлении аккаунтами и аутентификацией](#1-ошибки-в-управлении-аккаунтами-и-аутентификацией)
2. [Уязвимости при конфигурации SSH](#2-уязвимости-при-конфигурации-ssh)
3. [Ошибки конфигурации сети и firewall](#3-ошибки-конфигурации-сети-и-firewall)
4. [Ошибки при настройке обновлений и пакетов](#4-ошибки-при-настройке-обновлений-и-пакетов)
5. [Ошибки в управлении службами и демонами](#5-ошибки-в-управлении-службами-и-демонами)
6. [Ошибки с правами доступа и файлами](#6-ошибки-с-правами-доступа-и-файлами)
7. [Уязвимости веб-сервисов и приложений](#7-уязвимости-веб-сервисов-и-приложений)
8. [Ошибки в логировании и мониторинге](#8-ошибки-в-логировании-и-мониторинге)
9. [Уязвимости контейнеризации и виртуализации](#9-уязвимости-контейнеризации-и-виртуализации)
10. [Ошибки резервного копирования](#10-ошибки-резервного-копирования)
11. [Ошибки безопасности ядра и ОС](#11-ошибки-безопасности-ядра-и-ос)
12. [Физическая безопасность и доступ](#12-физическая-безопасность-и-доступ)

---

## 1. Ошибки в управлении аккаунтами и аутентификацией

### 1.1. Пароли и политика учётных записей

#### 1.1.1. Разрешены слабые пароли, нет PAM-ограничений

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование простых, предсказуемых или паролей по умолчанию в операционной системе.

**Примеры ошибок**:
- Пароли: `password`, `123456`, `admin`, `root`
- Пароли по умолчанию от производителя ОС
- Пароли, содержащие только буквы или только цифры
- Пароли, связанные с именем пользователя или названием компании
- Короткие пароли (менее 8 символов)

**Последствия**:
- Легкий брутфорс атак
- Компрометация учетных записей ОС
- Получение полного доступа к системе

**Исправление в Linux**:
```bash
# Настройка политики паролей в Linux
# /etc/security/pwquality.conf
minlen = 12
dcredit = -1
ucredit = -1
lcredit = -1
ocredit = -1
minclass = 3

# Использование сложных паролей
# Рекомендуется: минимум 12 символов, буквы (верхний/нижний регистр), цифры, спецсимволы
```

**Исправление в Windows**:
```powershell
# Настройка политики паролей через групповую политику
# Минимальная длина: 12 символов
# Требования к сложности: включены
Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\Netlogon\Parameters" -Name "MinimumPasswordLength" -Value 12
```

**Примеры в проекте**:
- Пользователь `admin` с паролем `admin123` на Ubuntu Server
- Пользователь `Administrator` с паролем `Admin123!` на Windows Server 2016

---

### 1.2. Разрешение входа root по SSH (Linux)

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Разрешение прямого входа пользователя root через SSH в Linux системах.

**Проблема**:
```bash
# НЕБЕЗОПАСНО
# /etc/ssh/sshd_config
PermitRootLogin yes
```

**Последствия**:
- Прямая атака на учетную запись с максимальными привилегиями
- Упрощение эксплуатации при компрометации пароля
- Нарушение принципа минимальных привилегий

**Исправление**:
```bash
# /etc/ssh/sshd_config
PermitRootLogin no
# Или только по ключам, если необходимо
PermitRootLogin prohibit-password

# Использование sudo для привилегированных операций
# Создание отдельного пользователя с sudo правами
sudo systemctl restart ssh
```

**Примеры в проекте**:
- В сценариях намеренно включен `PermitRootLogin yes` для демонстрации уязвимости

---

### 1.3. Аутентификация по паролю вместо ключей (Linux)

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Использование парольной аутентификации SSH вместо ключей в Linux системах.

**Проблема**:
```bash
# НЕБЕЗОПАСНО
# /etc/ssh/sshd_config
PasswordAuthentication yes
PubkeyAuthentication no
```

**Последствия**:
- Уязвимость к брутфорс атакам
- Перехват паролей при использовании небезопасных сетей
- Отсутствие защиты от перебора паролей

**Исправление**:
```bash
# /etc/ssh/sshd_config
PasswordAuthentication no
PubkeyAuthentication yes
AuthorizedKeysFile .ssh/authorized_keys

# Генерация SSH ключей
ssh-keygen -t ed25519 -C "user@example.com"
ssh-copy-id user@server

sudo systemctl restart ssh
```

---

### 1.4. Избыточные привилегии пользователей

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Предоставление пользователям прав администратора без необходимости.

**Проблема в Linux**:
```bash
# Все пользователи в группе sudo без ограничений
usermod -aG sudo username
```

**Проблема в Windows**:
```powershell
# Все пользователи в группе Administrators
Add-LocalGroupMember -Group "Administrators" -Member "username"
```

**Последствия**:
- Расширение последствий компрометации учетной записи
- Нарушение принципа минимальных привилегий
- Упрощение эскалации привилегий

**Исправление в Linux**:
```bash
# Ограничение sudo прав
# /etc/sudoers.d/custom
username ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart apache2
username ALL=(ALL) NOPASSWD: /usr/bin/systemctl status apache2
# Запрет выполнения других команд
```

**Исправление в Windows**:
```powershell
# Удаление из группы Administrators
Remove-LocalGroupMember -Group "Administrators" -Member "username"
# Создание отдельной группы с ограниченными правами
```

---

### 1.5. Отключен UAC в Windows

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Отключение User Account Control (UAC) в Windows системах.

**Проблема**:
```powershell
# Реестр Windows
# HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System
# EnableLUA = 0
```

**Последствия**:
- Приложения выполняются с правами администратора без запроса
- Упрощение эксплуатации вредоносного ПО
- Отсутствие защиты от несанкционированных изменений

**Исправление**:
```powershell
# Включение UAC
Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System" -Name "EnableLUA" -Value 1
# Требуется перезагрузка
```

---

## 2. Управление обновлениями

### 2.1. Отключение автоматических обновлений безопасности

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Отключение автоматических обновлений безопасности операционной системы.

**Проблема в Linux (Ubuntu/Debian)**:
```bash
# Отключение обновлений
systemctl disable unattended-upgrades
# Удаление файла конфигурации
rm /etc/apt/apt.conf.d/20auto-upgrades
```

**Проблема в Windows**:
```powershell
# Реестр Windows
# HKLM:\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate\AU
# NoAutoUpdate = 1
```

**Последствия**:
- Накопление неисправленных уязвимостей ОС
- Эксплуатация известных CVE
- Компрометация системы через уязвимости ОС

**Исправление в Linux**:
```bash
# Включение автоматических обновлений безопасности
# Ubuntu/Debian
apt install unattended-upgrades
dpkg-reconfigure -plow unattended-upgrades

# Настройка /etc/apt/apt.conf.d/50unattended-upgrades
Unattended-Upgrade::Automatic-Reboot "false";
Unattended-Upgrade::Remove-Unused-Kernel-Packages "true";
Unattended-Upgrade::Remove-Unused-Dependencies "true";
```

**Исправление в Windows**:
```powershell
# Включение автоматических обновлений
Set-ItemProperty -Path "HKLM:\SOFTWARE\Policies\Microsoft\Windows\WindowsUpdate\AU" -Name "NoAutoUpdate" -Value 0
# Или через групповую политику
```

**Примеры в проекте**:
- В сценариях намеренно отключены обновления для создания уязвимостей ОС

---

### 2.2. Использование устаревших версий ОС

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование версий операционных систем с известными критическими уязвимостями.

**Примеры**:
- Windows Server 2012 (поддержка прекращена)
- Ubuntu 16.04 LTS (поддержка прекращена)
- Windows 7 (поддержка прекращена)
- Старые версии ядра Linux с известными уязвимостями

**Последствия**:
- Эксплуатация известных уязвимостей ОС
- Отсутствие патчей безопасности
- Компрометация системы

**Исправление**:
```bash
# Linux - обновление до поддерживаемой версии
do-release-upgrade

# Windows - обновление до поддерживаемой версии
# Использование Windows Update или обновление до новой версии
```

---

## 3. Управление учетными записями

### 3.1. Хранение учетных данных в открытом виде

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Хранение паролей и других учетных данных в файлах с открытым доступом в файловой системе ОС.

**Проблема**:
```bash
# Пароли в скриптах
#!/bin/bash
mysql -u root -proot123 database

# Учетные данные в конфигурационных файлах
DB_PASSWORD=secret123

# Учетные данные в файлах с правами 644
echo "password=secret123" > /home/admin/credentials.txt
chmod 644 /home/admin/credentials.txt
```

**Последствия**:
- Компрометация при утечке файлов
- Доступ к системам злоумышленниками
- Нарушение конфиденциальности

**Исправление**:
```bash
# Использование переменных окружения
export DB_PASSWORD=$(cat /etc/secrets/db_password)

# Использование секретных хранилищ
# HashiCorp Vault, AWS Secrets Manager, Azure Key Vault

# Правильные права доступа
chmod 600 /home/admin/credentials.txt
chmod 600 ~/.ssh/id_rsa
```

**Примеры в проекте**:
- Файлы с учетными данными создаются намеренно для демонстрации

---

### 3.2. Использование учетных данных по умолчанию

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Оставление паролей и учетных записей по умолчанию в ОС.

**Примеры**:
- `admin/admin`
- `root/root`
- `administrator/password`
- Пароли от производителя оборудования

**Последствия**:
- Мгновенная компрометация при знании учетных данных
- Доступ к системам без взлома

**Исправление**:
```bash
# Обязательная смена паролей по умолчанию
# Использование уникальных сложных паролей
# Отключение или удаление учетных записей по умолчанию
```

---

### 3.3. Пароли никогда не истекают

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Настройка паролей так, чтобы они никогда не истекали.

**Проблема в Linux**:
```bash
# Установка пароля без срока истечения
chage -M -1 username
```

**Проблема в Windows**:
```powershell
# Установка пароля без срока истечения
Set-LocalUser -Name "username" -PasswordNeverExpires $true
```

**Последствия**:
- Долгосрочное использование скомпрометированных паролей
- Отсутствие ротации паролей

**Исправление**:
```bash
# Linux - установка срока истечения пароля
chage -M 90 username  # Пароль истекает через 90 дней
```

```powershell
# Windows - установка срока истечения пароля
Set-LocalUser -Name "username" -PasswordNeverExpires $false
```

---

## 4. Файловая система и права доступа

### 4.1. Неправильные права доступа к файлам

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Установка слишком открытых прав доступа к файлам и директориям в файловой системе ОС.

**Проблема в Linux**:
```bash
# Слишком открытые права
chmod 777 /srv/data
chmod 666 /etc/passwd
```

**Проблема в Windows**:
```powershell
# Права FullControl для Everyone
$acl = Get-Acl C:\Sensitive
$permission = "Everyone","FullControl","Allow"
$accessRule = New-Object System.Security.AccessControl.FileSystemAccessRule $permission
$acl.SetAccessRule($accessRule)
Set-Acl C:\Sensitive $acl
```

**Последствия**:
- Несанкционированный доступ к файлам
- Модификация критичных конфигураций ОС
- Утечка данных

**Исправление в Linux**:
```bash
# Принцип минимальных привилегий
chmod 755 /srv/data  # Владелец: rwx, группа: rx, другие: rx
chmod 644 /etc/passwd  # Владелец: rw, группа: r, другие: r
chmod 600 /etc/shadow  # Только владелец

# Использование ACL для сложных случаев
setfacl -m u:username:rwx /path/to/directory
```

**Исправление в Windows**:
```powershell
# Удаление прав Everyone
$acl = Get-Acl C:\Sensitive
$acl.SetAccessRuleProtection($true, $false)
$acl | Set-Acl
```

---

### 4.2. Хранение чувствительных данных в открытом виде

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Хранение паролей, ключей и других секретов в файлах с доступом для чтения в файловой системе ОС.

**Проблема**:
```bash
# Пароли в конфигурационных файлах
echo "password=secret123" > /etc/app/config.conf
chmod 644 /etc/app/config.conf

# Ключи в открытых файлах
cat ~/.ssh/id_rsa  # Публичный доступ
```

**Исправление**:
```bash
# Шифрование чувствительных данных
# Использование зашифрованных томов
# LUKS для шифрования дисков

# Правильные права доступа
chmod 600 /etc/app/config.conf
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/id_rsa.pub
```

**Примеры в проекте**:
- Файлы с учетными данными создаются с правами 644 для демонстрации

---

### 4.3. Уязвимости /etc/shadow и управления паролями (Linux)

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Множественные критические уязвимости, связанные с файлом `/etc/shadow`, который содержит хеши паролей всех пользователей системы.

#### 4.3.1. Неправильные права доступа на /etc/shadow

**Проблема**:
```bash
# НЕБЕЗОПАСНО
chmod 644 /etc/shadow  # Доступен для чтения всем
chmod 660 /etc/shadow  # Доступен группе shadow
# Группа shadow содержит лишних пользователей
```

**Стандарт**:
```bash
-r-------- 1 root shadow /etc/shadow  # Только root может читать
```

**Последствия**:
- Злоумышленник получает доступ к хешам паролей
- Возможность оффлайн-атак перебора
- Компрометация всех учетных записей системы

**Исправление**:
```bash
# Установка правильных прав
chmod 640 /etc/shadow
chown root:shadow /etc/shadow

# Удаление лишних пользователей из группы shadow
deluser username shadow
```

#### 4.3.2. Утечки /etc/shadow через неверные бэкапы

**Проблема**:
```bash
# Копии /etc/shadow в доступных каталогах
cp /etc/shadow /tmp/shadow_backup
cp /etc/shadow /home/admin/shadow_backup.txt
cp /etc/shadow /var/www/html/shadow_backup.txt
chmod 644 /tmp/shadow_backup  # Доступен всем
```

**Последствия**:
- Утечка хешей без компрометации сервера
- Доступ к хешам через веб-сервер или временные файлы

**Исправление**:
```bash
# Удаление небезопасных копий
rm /tmp/shadow_backup
rm /home/admin/shadow_backup.txt
rm /var/www/html/shadow_backup.txt

# Использование зашифрованных бэкапов
tar -czf - /etc/shadow | gpg --encrypt > shadow_backup.tar.gz.gpg
```

#### 4.3.3. Ошибки в конфигурации PAM

**Проблема**:
```bash
# /etc/pam.d/common-password
password [success=1 default=ignore] pam_unix.so obscure sha512 nullok
# nullok разрешает пустые пароли
```

**Последствия**:
- Возможность создания пользователей с пустыми паролями
- Игнорирование политики сложности паролей
- Слабые пароли в системе

**Исправление**:
```bash
# Удаление nullok
sed -i 's/nullok//g' /etc/pam.d/common-password

# Включение проверки сложности
# Добавить: password requisite pam_pwquality.so retry=3
```

#### 4.3.4. Использование уязвимых алгоритмов хеширования

**Проблема**:
```bash
# /etc/shadow
user:$1$salt$hash  # MD5 ($1$) - уязвим
user:$5$salt$hash  # SHA-256 ($5$) без rounds
user:$6$rounds=1000$salt$hash  # SHA-512 с низким rounds
```

**Последствия**:
- Хеши могут быть легко подобраны в оффлайн-режиме
- MD5 уязвим к коллизиям
- Низкое количество rounds ускоряет брутфорс

**Исправление**:
```bash
# Настройка SHA-512 с высоким количеством rounds
# /etc/pam.d/common-password
password [success=1 default=ignore] pam_unix.so obscure sha512 rounds=5000

# Принудительная смена паролей
chage -d 0 username
```

#### 4.3.5. Неправильные настройки парольной политики (shadow aging)

**Проблема**:
```bash
# /etc/shadow - пятое поле (максимальный срок действия)
user:$6$...:0:0:99999:7:::
# 99999 = пароль никогда не истекает
```

**Последствия**:
- Пароли не меняются годами
- Повышение вероятности компрометации
- Устаревшие пароли остаются активными

**Исправление**:
```bash
# Установка срока действия паролей (90 дней)
chage -M 90 username

# Установка минимального срока между сменами (7 дней)
chage -m 7 username
```

#### 4.3.6. Доступ к /etc/shadow через уязвимости SUID-приложений

**Проблема**:
```bash
# Уязвимый SUID скрипт
#!/bin/bash
cat /etc/shadow
# chmod 4755 /usr/local/bin/read_shadow
```

**Последствия**:
- Обход прямого запрета чтения /etc/shadow
- Локальные пользователи могут читать хеши

**Исправление**:
```bash
# Удаление уязвимых SUID скриптов
rm /usr/local/bin/read_shadow

# Проверка всех SUID программ
find /usr -perm -4000 -type f
```

#### 4.3.7. Утечка shadow через веб-приложения

**Проблема**:
```apache
# /etc/apache2/sites-available/vulnerable.conf
Alias /etc /etc
<Directory /etc>
    Options Indexes FollowSymLinks
    Require all granted
</Directory>
```

**Последствия**:
- Удалённый доступ к /etc/shadow через веб-сервер
- Directory traversal уязвимости

**Исправление**:
```bash
# Удаление небезопасных алиасов
rm /etc/apache2/sites-enabled/vulnerable.conf
# Ограничение доступа к системным файлам
```

#### 4.3.8. Ошибки при клонировании систем

**Проблема**:
- Клонирование VM вместе с /etc/shadow
- Одинаковые хеши паролей на нескольких серверах
- Тестовые среды имеют те же пароли, что и прод

**Последствия**:
- Один взлом ведёт к цепочке компрометаций
- Компрометация всех клонированных систем

**Исправление**:
```bash
# Принудительная смена всех паролей после клонирования
for user in $(cut -d: -f1 /etc/passwd); do
    if [ "$user" != "root" ]; then
        chage -d 0 $user
    fi
done
```

**Примеры в проекте**:
- Все перечисленные уязвимости реализованы в сценариях для обучения

---

## 5. Встроенные уязвимости ОС

### 5.1. SMBv1 включен (Windows/Linux)

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Включение устаревшего протокола SMBv1, уязвимого к атакам типа EternalBlue.

**Проблема в Windows**:
```powershell
# Проверка статуса SMBv1
Get-WindowsOptionalFeature -Online -FeatureName SMB1Protocol
# Status: Enabled
```

**Проблема в Linux (Samba)**:
```bash
# /etc/samba/smb.conf
[global]
server min protocol = NT1
```

**Последствия**:
- Уязвимость к атакам EternalBlue
- Компрометация системы через уязвимость ОС
- Распространение вредоносного ПО

**Исправление в Windows**:
```powershell
# Отключение SMBv1
Disable-WindowsOptionalFeature -Online -FeatureName SMB1Protocol -NoRestart
```

**Исправление в Linux**:
```bash
# Отключение SMBv1 в Samba
# /etc/samba/smb.conf
[global]
server min protocol = SMB2

sudo systemctl restart smbd
```

---

### 5.2. Отключен брандмауэр ОС

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Отключение встроенного брандмауэра операционной системы.

**Проблема в Linux**:
```bash
# Отключение UFW
ufw disable
# Или не настроен firewalld
systemctl stop firewalld
systemctl disable firewalld
```

**Проблема в Windows**:
```powershell
# Отключение брандмауэра Windows
Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled False
```

**Последствия**:
- Открытый доступ ко всем портам
- Отсутствие фильтрации сетевого трафика
- Упрощение сетевых атак

**Исправление в Linux**:
```bash
# Включение и настройка UFW
ufw default deny incoming
ufw default allow outgoing
ufw allow 22/tcp
ufw enable
```

**Исправление в Windows**:
```powershell
# Включение брандмауэра
Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled True
```

---

### 5.3. Отключен антивирус/защита ОС

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Отключение встроенной защиты операционной системы.

**Проблема в Windows**:
```powershell
# Отключение Windows Defender
Set-MpPreference -DisableRealtimeMonitoring $true
```

**Последствия**:
- Отсутствие защиты от вредоносного ПО
- Упрощение эксплуатации
- Распространение вредоносного ПО

**Исправление в Windows**:
```powershell
# Включение Windows Defender
Set-MpPreference -DisableRealtimeMonitoring $false
```

---

### 5.4. RDP включен без ограничений (Windows)

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Включение Remote Desktop Protocol без ограничений доступа.

**Проблема**:
```powershell
# Включение RDP
Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0
# Отсутствие ограничений по IP
# Отсутствие Network Level Authentication
```

**Последствия**:
- Брутфорс атаки на RDP
- Компрометация через слабые пароли
- Прямой доступ к рабочему столу

**Исправление**:
```powershell
# Включение Network Level Authentication
Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 1
# Ограничение доступа по IP через брандмауэр
```

---

### 5.5. Уязвимости конфигурации по умолчанию

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Операционные системы часто поставляются с небезопасными настройками по умолчанию.

#### Ubuntu/Debian:
1. **Открытый доступ к информации о системе**:
   ```bash
   # /proc и /sys могут раскрывать информацию
   # Отключение ненужных модулей ядра
   ```

2. **Слабая конфигурация AppArmor/SELinux**:
   ```bash
   # Ubuntu использует AppArmor, но не все сервисы защищены
   # Необходимо настраивать профили для критичных приложений
   ```

3. **Открытые сетевые сервисы**:
   - Avahi (mDNS)
   - CUPS (печать)
   - Необходимо отключать неиспользуемые сервисы

#### Windows:
1. **Отключенные функции безопасности по умолчанию**:
   - UAC может быть отключен
   - Брандмауэр может быть не настроен
   - Windows Defender может быть отключен

2. **Открытые порты**:
   - RDP может быть включен
   - SMB может быть открыт
   - Необходимо настраивать правила брандмауэра

---

## 6. Уязвимости Windows из-за ошибок администратора

### 6.1. Уязвимости аутентификации и управления учётными записями

#### 6.1.1. Ошибки с паролями

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование слабых или стандартных паролей в Windows системах.

**Проблемы**:
- Использование слабых или стандартных паролей
- Не включена политика сложных паролей
- Нет запрета на повторяющиеся/ранее использованные пароли
- Не настроены сроки действия паролей

**Примеры ошибок**:
```powershell
# Слабые пароли
Administrator: Admin123!
admin: admin123
user1: password123

# Отключение политики сложности паролей
Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\Netlogon\Parameters" -Name "MinimumPasswordLength" -Value 0
```

**Последствия**:
- Легкий брутфорс атак
- Компрометация учетных записей
- Получение полного доступа к системе и домену

**Исправление**:
```powershell
# Настройка политики паролей через GPO
# Минимальная длина: 12 символов
# Требования к сложности: включены
# Запрет повторяющихся паролей: последние 24
# Срок действия пароля: 90 дней
Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\Netlogon\Parameters" -Name "MinimumPasswordLength" -Value 12
```

#### 6.1.2. Ошибки с административными учетными записями

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Неправильное управление административными учетными записями.

**Проблемы**:
- Использование одного и того же локального пароля на всех серверах
- Работа в сети под учёткой Domain Admin для обычных задач
- Не создано разделение ролей: админ домена = админ серверов = админ приложений
- Нет уникальных админ-учёток для конкретных задач (PAW / tiering)

**Последствия**:
- Компрометация одной учетной записи приводит к компрометации всей инфраструктуры
- Нарушение принципа минимальных привилегий
- Упрощение горизонтальной эскалации привилегий

**Исправление**:
```powershell
# Создание отдельных учетных записей для разных задач
# Использование Privileged Access Workstations (PAW)
# Разделение ролей: Domain Admins, Server Admins, Application Admins
# Использование Just-In-Time (JIT) доступа
```

#### 6.1.3. Ошибки MFA

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Отсутствие многофакторной аутентификации для критичных учетных записей.

**Проблемы**:
- MFA не настроена ни для одной критичной учётной записи
- Не защищены Emergency / Break-glass аккаунты

**Исправление**:
```powershell
# Настройка MFA через Azure AD или AD FS
# Защита всех административных учетных записей
# Использование аппаратных токенов для Break-glass аккаунтов
```

---

### 6.2. Ошибки в настройке политик безопасности (GPO)

#### 6.2.1. Неправильные GPO, влияющие на безопасность

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Неправильная настройка групповых политик безопасности.

**Проблемы**:
- Разрешены WMI/PowerShell Remoting без ограничений
- Отключён аудит событий безопасности
- Разрешено хранение паролей в GPP (Group Policy Preferences)
- Нет ограничения на установку неподписанных драйверов и приложений

**Примеры ошибок**:
```powershell
# Разрешение PowerShell Remoting для всех
Enable-PSRemoting -Force
Set-Item WSMan:\localhost\Client\TrustedHosts -Value "*"

# Хранение паролей в GPP (уязвимость CVE-2014-1812)
# Пароли хранятся в открытом виде в SYSVOL
```

**Последствия**:
- Компрометация учетных данных через GPP
- Несанкционированный удаленный доступ
- Отсутствие аудита безопасности

**Исправление**:
```powershell
# Удаление паролей из GPP
# Использование Group Managed Service Accounts (gMSA)
# Включение аудита безопасности
# Ограничение PowerShell Remoting по IP и пользователям
```

#### 6.2.2. Слишком широкие политики

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Применение политик безопасности ко всем OU вместо нужных групп.

**Проблемы**:
- GPO применяются ко всем OU вместо нужных групп
- Чрезмерные права в "Allow log on locally", "Allow log on through RDP"

**Исправление**:
```powershell
# Применение GPO только к нужным группам
# Ограничение прав "Allow log on locally" и "Allow log on through RDP"
# Использование фильтров безопасности в GPO
```

---

### 6.3. Ошибки в управлении правами (ACL, делегирование, группы)

#### 6.3.1. Слишком широкие права на AD-объекты

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Предоставление избыточных прав на объекты Active Directory.

**Проблемы**:
- Authenticated Users имеют доступ на изменение критичных объектов
- WriteDacl/WriteOwner на чувствительные OU или группы
- Делегирование настроено неправильно, позволяя повышать привилегии

**Последствия**:
- Компрометация всей инфраструктуры AD
- Эскалация привилегий
- Несанкционированные изменения в домене

**Исправление**:
```powershell
# Проверка и исправление ACL на AD объектах
# Удаление прав Authenticated Users на критичные объекты
# Ограничение делегирования
# Регулярный аудит прав доступа
```

#### 6.3.2. Чрезмерное использование привилегированных групп

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Избыточное использование привилегированных групп Active Directory.

**Проблемы**:
- Domain Admins используются для всего подряд
- Administrators/SysAdmins добавлены в слишком многие системы
- Пользователи остаются в группе Enterprise Admins без необходимости

**Исправление**:
```powershell
# Удаление пользователей из привилегированных групп
# Использование принципа минимальных привилегий
# Создание отдельных групп для разных задач
# Регулярный аудит членства в группах
```

#### 6.3.3. Unconstrained Delegation

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Включение неограниченного делегирования Kerberos.

**Проблемы**:
- У хостов включено "Trust this computer for delegation to any service"
- Присутствуют устаревшие SPN на сервисных аккаунтах

**Последствия**:
- Компрометация учетных данных Kerberos
- Эскалация привилегий через делегирование

**Исправление**:
```powershell
# Отключение Unconstrained Delegation
# Использование Constrained Delegation или Resource-Based Constrained Delegation
# Очистка устаревших SPN
```

---

### 6.4. Уязвимости из-за неправильной конфигурации сети

#### 6.4.1. Ошибки DNS

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Неправильная настройка DNS сервера.

**Проблемы**:
- Разрешено динамическое обновление DNS без ограничений
- Открытый DNS к внешней сети
- Неправильные ACL на DNS-записи, что может позволить "DNS poisoning"

**Исправление**:
```powershell
# Ограничение динамических обновлений DNS
# Настройка безопасных ACL на DNS записи
# Ограничение доступа к DNS серверу
```

#### 6.4.2. Ошибки в Firewall

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Неправильная настройка брандмауэра Windows.

**Проблемы**:
- Допущены широкие правила: Any → Any
- Нет сегментации: контроллеры домена (DC) доступны из всех сетей
- RDP открыт наружу

**Исправление**:
```powershell
# Настройка правил брандмауэра
# Ограничение доступа к DC только из определенных сетей
# Закрытие RDP для внешних сетей
# Использование Network Security Groups
```

#### 6.4.3. SMB и протоколы старых версий

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование устаревших и небезопасных протоколов.

**Проблемы**:
- SMBv1 включён
- NTLMv1 разрешён
- LDAP без TLS

**Исправление**:
```powershell
# Отключение SMBv1
Disable-WindowsOptionalFeature -Online -FeatureName SMB1Protocol

# Отключение NTLMv1, использование только NTLMv2
# Включение LDAPS (LDAP over TLS)
```

---

### 6.5. Ошибки при настройке контроллеров домена (DC)

#### 6.5.1. DC используются как обычные рабочие сервера

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование контроллеров домена для других задач.

**Проблемы**:
- Развёрнуты роли DHCP, SQL, файловый сервер, приложения — всё на DC
- Пользователи могут заходить на DC локально или по RDP

**Последствия**:
- Повышение риска компрометации DC
- Нарушение принципа разделения ролей
- Упрощение атак на контроллеры домена

**Исправление**:
```powershell
# Удаление всех ролей кроме AD DS с DC
# Запрет локального входа на DC
# Запрет RDP на DC
# Использование отдельного сервера для других ролей
```

#### 6.5.2. Не настроена безопасная репликация

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Небезопасная настройка репликации между контроллерами домена.

**Проблемы**:
- Разрешён RPC во все стороны
- DFSR или SYSVOL не защищены надлежащими разрешениями

**Исправление**:
```powershell
# Настройка безопасной репликации
# Ограничение RPC только между DC
# Проверка разрешений на SYSVOL
```

#### 6.5.3. Отсутствие контроля за физическим доступом

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Недостаточный контроль физического доступа к контроллерам домена.

**Проблемы**:
- Серверы или виртуальные машины DC доступны широкому кругу сотрудников

**Исправление**:
```powershell
# Ограничение физического доступа к DC
# Использование BitLocker для шифрования дисков DC
# Настройка BIOS/UEFI паролей
```

---

### 6.6. Ошибки в работе с обновлениями и антивирусом

#### 6.6.1. Отсутствие регулярного обновления

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Отсутствие системы управления обновлениями.

**Проблемы**:
- Нет WSUS/SCCM или аналогичной системы управления патчами
- DC и критичные сервисы не обновляются месяцами

**Исправление**:
```powershell
# Настройка WSUS или SCCM
# Регулярное применение обновлений безопасности
# Тестирование обновлений перед применением
```

#### 6.6.2. Антивирус настроен неправильно

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Неправильная настройка антивирусного ПО.

**Проблемы**:
- Исключения в антивирусе включают слишком много путей
- Не включена защита от эксплуатации (ASR rules)

**Исправление**:
```powershell
# Минимизация исключений в антивирусе
# Включение ASR (Attack Surface Reduction) правил
# Регулярное обновление сигнатур
```

---

### 6.7. Уязвимости логирования, мониторинга и аудита

#### 6.7.1. Логирование минимальное или отсутствует

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Недостаточное логирование событий безопасности.

**Проблемы**:
- Event logs очищаются автоматически
- Нет отправки логов на SIEM или централизованный лог-сервер
- Аудит отключён или настроен только частично

**Исправление**:
```powershell
# Включение аудита безопасности
# Настройка отправки логов на SIEM
# Настройка ротации логов
# Включение аудита критичных событий
```

#### 6.7.2. Нет мониторинга аномалий

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Отсутствие мониторинга подозрительной активности.

**Проблемы**:
- Не отслеживаются входы администраторов
- Не отслеживаются изменения в чувствительных группах
- Нет мониторинга NTDS.dit или попыток его копирования

**Исправление**:
```powershell
# Настройка мониторинга входов администраторов
# Мониторинг изменений в группах Domain Admins, Enterprise Admins
# Мониторинг доступа к NTDS.dit
# Использование Microsoft Defender for Identity
```

---

### 6.8. Ошибки в резервном копировании

#### 6.8.1. Бэкапы лежат в домене и не защищены

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Небезопасное хранение резервных копий.

**Проблемы**:
- Нет offline/immutable backup
- Сами бэкапы не зашифрованы

**Исправление**:
```powershell
# Хранение бэкапов вне домена
# Шифрование бэкапов
# Использование immutable storage
# Регулярное тестирование восстановления
```

#### 6.8.2. Не делаются бэкапы критичных объектов

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Отсутствие резервных копий критичных компонентов.

**Проблемы**:
- SYSVOL не бэкапится
- DNS не бэкапится
- GPO не бэкапятся
- Образы DC не бэкапятся

**Исправление**:
```powershell
# Регулярное бэкапирование SYSVOL, DNS, GPO
# Бэкапирование образов DC
# Тестирование восстановления
```

---

### 6.9. Ошибки при работе с сервисными аккаунтами

#### 6.9.1. Сервисные аккаунты с привилегиями Domain Admin

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование привилегированных учетных записей для сервисов.

**Проблемы**:
- Приложения работают от имени слишком высоко привилегированных аккаунтов

**Исправление**:
```powershell
# Использование Group Managed Service Accounts (gMSA)
# Принцип минимальных привилегий для сервисных аккаунтов
# Регулярный аудит сервисных аккаунтов
```

#### 6.9.2. Пароли сервисных аккаунтов не меняются годами

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Отсутствие ротации паролей сервисных аккаунтов.

**Проблемы**:
- Нет автоматизации (gMSA)

**Исправление**:
```powershell
# Использование gMSA (автоматическая ротация паролей)
# Миграция обычных сервисных аккаунтов на gMSA
```

#### 6.9.3. SPN настроены некорректно

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Неправильная настройка Service Principal Names.

**Проблемы**:
- У счетов с SPN слишком широкие разрешения

**Исправление**:
```powershell
# Проверка и исправление SPN
# Ограничение разрешений для аккаунтов с SPN
```

---

### 6.10. Устаревшие и небезопасные технологии

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование устаревших и небезопасных технологий.

**Проблемы**:
- NTLM вместо Kerberos
- FRS вместо DFSR для SYSVOL
- Windows Server 2008/2012 без поддержки безопасности
- Открытые SMB-сервера без подписывания

**Исправление**:
```powershell
# Отключение NTLM, использование только Kerberos
# Миграция с FRS на DFSR
# Обновление до поддерживаемых версий Windows Server
# Включение SMB signing
```

---

## Чек-лист безопасности ОС

### При развертывании новой системы:

- [ ] Изменены все пароли по умолчанию
- [ ] Отключен root login по SSH (Linux)
- [ ] Настроена аутентификация по ключам (Linux)
- [ ] Настроен брандмауэр (только необходимые порты)
- [ ] Отключены неиспользуемые сервисы
- [ ] Включены автоматические обновления безопасности
- [ ] Настроена политика паролей
- [ ] Проверены права доступа к файлам
- [ ] Удалены учетные данные из конфигураций
- [ ] Отключен SMBv1 (если не требуется)
- [ ] Включен антивирус/защита ОС
- [ ] Настроен UAC (Windows)
- [ ] Проведен аудит безопасности ОС

---

## Инструменты для проверки уязвимостей ОС

### Сканеры уязвимостей ОС:

1. **Lynis** — аудит безопасности Linux систем
2. **OpenSCAP** — проверка соответствия стандартам безопасности ОС
3. **CIS-CAT** — проверка соответствия CIS Benchmarks для ОС
4. **Microsoft Security Compliance Toolkit** — проверка Windows систем

### Аудит конфигураций ОС:

1. **Ansible-lint** — проверка Ansible конфигураций
2. **Terraform validate** — валидация Terraform конфигураций
3. **PowerShell DSC** — проверка конфигураций Windows

---

## Заключение

Большинство ошибок безопасности ОС возникают из-за:
1. **Незнания** — администраторы не знают о рисках
2. **Удобства** — выбор простых решений вместо безопасных
3. **Времени** — отсутствие времени на правильную настройку ОС
4. **Отсутствия политик** — нет четких требований безопасности ОС

**Рекомендации**:
- Регулярное обучение администраторов по безопасности ОС
- Использование чек-листов безопасности ОС
- Автоматизация проверок конфигураций ОС
- Внедрение принципа минимальных привилегий
- Регулярный аудит безопасности ОС

---

## Дополнительные ресурсы

- [CIS Benchmarks](https://www.cisecurity.org/cis-benchmarks/) — рекомендации по безопасности ОС
- [NIST Cybersecurity Framework](https://www.nist.gov/cyberframework)
- [Microsoft Security Baselines](https://docs.microsoft.com/en-us/windows/security/threat-protection/windows-security-configuration-framework/security-baselines)
- [CVE Database](https://cve.mitre.org/) — база данных уязвимостей ОС
