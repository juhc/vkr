# Критические ошибки безопасности при настройке ОС

## Введение

Данный документ описывает наиболее распространенные и критические ошибки настройки безопасности, которые администраторы допускают при развертывании операционных систем. Эти ошибки часто становятся причиной компрометации систем и утечек данных.

**⚠️ ВНИМАНИЕ**: Данный документ создан для образовательных целей. Все описанные уязвимости используются в учебных сценариях проекта для обучения навыкам кибербезопасности.

---

## Категории ошибок

1. [Аутентификация и авторизация](#1-аутентификация-и-авторизация)
2. [Сетевая безопасность](#2-сетевая-безопасность)
3. [Конфигурация сервисов](#3-конфигурация-сервисов)
4. [Управление обновлениями](#4-управление-обновлениями)
5. [Управление учетными данными](#5-управление-учетными-данными)
6. [Логирование и мониторинг](#6-логирование-и-мониторинг)
7. [Файловая система и права доступа](#7-файловая-система-и-права-доступа)
8. [Встроенные уязвимости ОС](#8-встроенные-уязвимости-ос)

---

## 1. Аутентификация и авторизация

### 1.1. Слабые пароли

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование простых, предсказуемых или паролей по умолчанию.

**Примеры ошибок**:
- Пароли: `password`, `123456`, `admin`, `root`
- Пароли по умолчанию от производителя
- Пароли, содержащие только буквы или только цифры
- Пароли, связанные с именем пользователя или названием компании

**Последствия**:
- Легкий брутфорс атак
- Компрометация учетных записей
- Получение полного доступа к системе

**Исправление**:
```bash
# Настройка политики паролей в Linux
# /etc/security/pwquality.conf
minlen = 12
dcredit = -1
ucredit = -1
lcredit = -1
ocredit = -1
minclass = 3

# Использование сложных паролей
# Рекомендуется: минимум 12 символов, буквы (верхний/нижний регистр), цифры, спецсимволы
```

**Примеры в проекте**:
- MySQL root пароль: `root123`
- Пользовательские пароли: `admin123`, `dev123`

---

### 1.2. Разрешение входа root по SSH

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Разрешение прямого входа пользователя root через SSH.

**Проблема**:
```bash
# НЕБЕЗОПАСНО
PermitRootLogin yes
```

**Последствия**:
- Прямая атака на учетную запись с максимальными привилегиями
- Упрощение эксплуатации при компрометации пароля

**Исправление**:
```bash
# /etc/ssh/sshd_config
PermitRootLogin no
PermitRootLogin prohibit-password  # Только по ключам, если необходимо

# Использование sudo для привилегированных операций
# Создание отдельного пользователя с sudo правами
```

**Примеры в проекте**:
- В сценариях намеренно включен `PermitRootLogin yes` для демонстрации уязвимости

---

### 1.3. Аутентификация по паролю вместо ключей

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Использование парольной аутентификации SSH вместо ключей.

**Проблема**:
```bash
# НЕБЕЗОПАСНО
PasswordAuthentication yes
PubkeyAuthentication no
```

**Последствия**:
- Уязвимость к брутфорс атакам
- Перехват паролей при использовании небезопасных сетей

**Исправление**:
```bash
# /etc/ssh/sshd_config
PasswordAuthentication no
PubkeyAuthentication yes
AuthorizedKeysFile .ssh/authorized_keys

# Генерация SSH ключей
ssh-keygen -t ed25519 -C "user@example.com"
ssh-copy-id user@server
```

---

### 1.4. Избыточные привилегии пользователей

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Предоставление пользователям прав администратора без необходимости.

**Проблема**:
```bash
# Все пользователи в группе sudo без ограничений
usermod -aG sudo username
```

**Последствия**:
- Расширение последствий компрометации учетной записи
- Нарушение принципа минимальных привилегий

**Исправление**:
```bash
# Ограничение sudo прав
# /etc/sudoers.d/custom
username ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart apache2
username ALL=(ALL) NOPASSWD: /usr/bin/systemctl status apache2
# Запрет выполнения других команд
```

---

## 2. Сетевая безопасность

### 2.1. Открытые порты и сервисы

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Открытие ненужных портов и сервисов, доступных извне.

**Примеры ошибок**:
- Открытые порты управления (SSH на стандартном порту, RDP, VNC)
- Неиспользуемые сервисы (FTP, Telnet, старые версии протоколов)
- Порты баз данных, доступные из интернета

**Последствия**:
- Увеличение поверхности атаки
- Прямой доступ к уязвимым сервисам
- Упрощение разведки злоумышленниками

**Исправление**:
```bash
# Использование брандмауэра
# UFW (Ubuntu)
ufw default deny incoming
ufw default allow outgoing
ufw allow 22/tcp  # Только необходимые порты
ufw allow 80/tcp
ufw allow 443/tcp
ufw enable

# firewalld (CentOS/RHEL)
firewall-cmd --permanent --add-service=ssh
firewall-cmd --permanent --add-service=http
firewall-cmd --permanent --add-service=https
firewall-cmd --reload

# Отключение ненужных сервисов
systemctl disable telnet
systemctl disable ftp
systemctl stop telnet
systemctl stop ftp
```

**Примеры в проекте**:
- MySQL порт 3306 открыт для всех
- Множество открытых портов без фильтрации

---

### 2.2. Отсутствие сегментации сети

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Размещение всех серверов в одной сети без изоляции.

**Проблема**:
- Веб-серверы, базы данных и рабочие станции в одной подсети
- Отсутствие DMZ для публичных сервисов
- Нет разделения на production, staging, development

**Последствия**:
- Латеральное перемещение после первоначальной компрометации
- Доступ к критичным системам с менее защищенных
- Отсутствие контроля сетевого трафика

**Исправление**:
```
Сетевая архитектура:
- DMZ (192.168.10.0/24): Публичные сервисы (веб, почта)
- Internal (192.168.20.0/24): Внутренние сервисы (БД, файловые серверы)
- Management (192.168.40.0/24): Управление инфраструктурой
- Development (192.168.30.0/24): Окружения разработки

Использование VLAN и брандмауэров для изоляции
```

**Примеры в проекте**:
- Сценарий демонстрирует правильную сегментацию с отдельными сетями

---

### 2.3. Использование небезопасных протоколов

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Использование протоколов без шифрования или с известными уязвимостями.

**Примеры**:
- FTP вместо SFTP/FTPS
- HTTP вместо HTTPS
- Telnet вместо SSH
- SMBv1 (уязвим к EternalBlue)
- Старые версии TLS/SSL

**Последствия**:
- Перехват трафика
- Модификация данных в передаче
- Компрометация учетных данных

**Исправление**:
```bash
# Отключение небезопасных протоколов
# Отключение SMBv1
echo "install smb /bin/false" >> /etc/modprobe.d/blacklist.conf

# Использование только TLS 1.2+
# Apache
SSLProtocol all -SSLv2 -SSLv3 -TLSv1 -TLSv1.1

# Nginx
ssl_protocols TLSv1.2 TLSv1.3;
```

---

### 2.4. Открытый рекурсивный DNS

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Настройка DNS сервера для выполнения рекурсивных запросов для любых клиентов.

**Проблема**:
```bash
# BIND9 небезопасная конфигурация
options {
    recursion yes;
    allow-recursion { any; };
};
```

**Последствия**:
- DNS amplification атаки
- Утечка информации о внутренней сети
- Использование сервера для DDoS атак

**Исправление**:
```bash
# BIND9 безопасная конфигурация
options {
    recursion yes;
    allow-recursion { 
        192.168.20.0/24;  # Только внутренняя сеть
        127.0.0.1;
    };
    allow-query { 
        192.168.20.0/24;
    };
};
```

**Примеры в проекте**:
- DNS сервер настроен с `allow-recursion { any; }` для демонстрации

---

## 3. Конфигурация сервисов

### 3.1. Небезопасная конфигурация веб-серверов

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Раскрытие информации о версии и конфигурации веб-сервера.

**Проблема**:
```apache
# Apache небезопасная конфигурация
ServerTokens Full
ServerSignature On
```

**Последствия**:
- Раскрытие версии ПО для целенаправленных атак
- Упрощение разведки злоумышленниками

**Исправление**:
```apache
# Apache безопасная конфигурация
ServerTokens Prod
ServerSignature Off

# Скрытие версии в заголовках
Header unset Server
Header always unset X-Powered-By
```

**Примеры в проекте**:
- Веб-сервер настроен с `ServerTokens Full` для демонстрации

---

### 3.2. Небезопасная конфигурация баз данных

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Разрешение удаленного доступа к базе данных без ограничений.

**Проблема**:
```sql
-- MySQL небезопасная конфигурация
bind-address = 0.0.0.0
-- Разрешение доступа root с любого хоста
GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' IDENTIFIED BY 'weakpassword';
```

**Последствия**:
- Прямой доступ к данным
- SQL инъекции
- Утечка чувствительной информации

**Исправление**:
```sql
-- MySQL безопасная конфигурация
bind-address = 127.0.0.1  # Только локальный доступ

-- Создание пользователя с ограниченными правами
CREATE USER 'app_user'@'192.168.10.10' IDENTIFIED BY 'strong_password';
GRANT SELECT, INSERT, UPDATE ON app_db.* TO 'app_user'@'192.168.10.10';

-- Использование SSL для соединений
REQUIRE SSL;
```

**Примеры в проекте**:
- MySQL настроен с `bind-address = 0.0.0.0` и слабым паролем root

---

### 3.3. Открытые файловые шары

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Настройка файловых шаров с правами записи для всех пользователей.

**Проблема**:
```bash
# Samba небезопасная конфигурация
[public-share]
path = /srv/public
browsable = yes
writable = yes
guest ok = yes
```

**Последствия**:
- Несанкционированный доступ к файлам
- Загрузка вредоносного ПО
- Утечка данных

**Исправление**:
```bash
# Samba безопасная конфигурация
[secure-share]
path = /srv/secure
browsable = no
writable = yes
valid users = @smbgroup
write list = @smbgroup
read list = @smbgroup
guest ok = no
force user = smbuser
force group = smbgroup
```

**Примеры в проекте**:
- Файловый сервер настроен с открытыми шарами для демонстрации

---

## 4. Управление обновлениями

### 4.1. Отключение автоматических обновлений

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Отключение автоматических обновлений безопасности.

**Проблема**:
```bash
# Отключение обновлений
systemctl disable unattended-upgrades
apt-mark hold package-name
```

**Последствия**:
- Накопление неисправленных уязвимостей
- Эксплуатация известных CVE
- Компрометация системы

**Исправление**:
```bash
# Включение автоматических обновлений безопасности
# Ubuntu/Debian
apt install unattended-upgrades
dpkg-reconfigure -plow unattended-upgrades

# Настройка /etc/apt/apt.conf.d/50unattended-upgrades
Unattended-Upgrade::Automatic-Reboot "false";
Unattended-Upgrade::Remove-Unused-Kernel-Packages "true";
Unattended-Upgrade::Remove-Unused-Dependencies "true";
```

**Примеры в проекте**:
- В сценариях намеренно отключены обновления для создания уязвимостей

---

### 4.2. Использование устаревших версий ПО

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Использование версий ПО с известными критическими уязвимостями.

**Примеры**:
- Log4j 2.14.1 (CVE-2021-44228 - Log4Shell)
- Apache Struts 2.3.x (CVE-2017-5638)
- OpenSSL старые версии (Heartbleed)

**Последствия**:
- Эксплуатация известных уязвимостей
- Компрометация системы
- Утечка данных

**Исправление**:
```bash
# Регулярное обновление ПО
apt update && apt upgrade -y

# Проверка версий уязвимых библиотек
# Использование сканеров уязвимостей
# Dependency-check, Snyk, OWASP Dependency-Check
```

**Примеры в проекте**:
- Веб-сервер использует Log4j 2.14.1 для демонстрации Log4Shell

---

## 5. Управление учетными данными

### 5.1. Хранение учетных данных в открытом виде

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Хранение паролей, ключей API и других учетных данных в открытом виде.

**Проблема**:
```bash
# Пароли в скриптах
#!/bin/bash
mysql -u root -proot123 database

# Учетные данные в конфигурационных файлах
DB_PASSWORD=secret123

# Учетные данные в репозиториях Git
echo "password=secret123" >> config.txt
git commit -a -m "Add config"
```

**Последствия**:
- Компрометация при утечке файлов
- Доступ к системам злоумышленниками
- Нарушение конфиденциальности

**Исправление**:
```bash
# Использование переменных окружения
export DB_PASSWORD=$(cat /etc/secrets/db_password)

# Использование секретных хранилищ
# HashiCorp Vault, AWS Secrets Manager, Azure Key Vault

# Использование Ansible Vault
ansible-vault encrypt_string 'secret123' --name 'db_password'

# Использование .env файлов (не коммитить в Git)
# Добавление в .gitignore
```

**Примеры в проекте**:
- Файлы с учетными данными создаются намеренно для демонстрации

---

### 5.2. Использование учетных данных по умолчанию

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Оставление паролей и учетных записей по умолчанию.

**Примеры**:
- `admin/admin`
- `root/root`
- `administrator/password`
- Пароли от производителя оборудования

**Последствия**:
- Мгновенная компрометация при знании учетных данных
- Доступ к системам без взлома

**Исправление**:
```bash
# Обязательная смена паролей по умолчанию
# Использование уникальных сложных паролей
# Отключение или удаление учетных записей по умолчанию
```

---

## 6. Логирование и мониторинг

### 6.1. Отсутствие логирования критичных событий

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Не настроено логирование важных событий безопасности.

**Проблема**:
- Нет логирования неудачных попыток входа
- Нет логирования изменений конфигураций
- Нет логирования доступа к чувствительным данным

**Последствия**:
- Невозможность обнаружения атак
- Отсутствие доказательств при инцидентах
- Затрудненное расследование

**Исправление**:
```bash
# Настройка auditd (Linux)
# /etc/audit/auditd.conf
log_file = /var/log/audit/audit.log
max_log_file = 50
num_logs = 5

# Мониторинг неудачных входов
# /etc/audit/rules.d/audit.rules
-w /var/log/faillog -p wa -k logins
-w /var/log/lastlog -p wa -k logins

# Централизованное логирование
# Использование syslog, rsyslog, или ELK Stack
```

---

### 6.2. Отсутствие мониторинга безопасности

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Не настроен мониторинг подозрительной активности.

**Последствия**:
- Позднее обнаружение атак
- Отсутствие раннего предупреждения
- Невозможность быстрого реагирования

**Исправление**:
```bash
# Настройка IDS/IPS
# Snort, Suricata, OSSEC

# Мониторинг файловой целостности
# AIDE, Tripwire

# Системы обнаружения аномалий
# Fail2ban для защиты от брутфорса
```

---

## 7. Файловая система и права доступа

### 7.1. Неправильные права доступа к файлам

**Критичность**: 🟡 ВЫСОКАЯ

**Описание**: Установка слишком открытых прав доступа к файлам и директориям.

**Проблема**:
```bash
# Слишком открытые права
chmod 777 /srv/data
chmod 666 /etc/passwd
```

**Последствия**:
- Несанкционированный доступ к файлам
- Модификация критичных конфигураций
- Утечка данных

**Исправление**:
```bash
# Принцип минимальных привилегий
chmod 755 /srv/data  # Владелец: rwx, группа: rx, другие: rx
chmod 644 /etc/passwd  # Владелец: rw, группа: r, другие: r
chmod 600 /etc/shadow  # Только владелец

# Использование ACL для сложных случаев
setfacl -m u:username:rwx /path/to/directory
```

---

### 7.2. Хранение чувствительных данных в открытом виде

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Хранение паролей, ключей и других секретов в файлах с доступом для чтения.

**Проблема**:
```bash
# Пароли в конфигурационных файлах
echo "password=secret123" > /etc/app/config.conf
chmod 644 /etc/app/config.conf

# Ключи в открытых файлах
cat ~/.ssh/id_rsa  # Публичный доступ
```

**Исправление**:
```bash
# Шифрование чувствительных данных
# Использование зашифрованных томов
# LUKS для шифрования дисков

# Правильные права доступа
chmod 600 /etc/app/config.conf
chmod 600 ~/.ssh/id_rsa
chmod 644 ~/.ssh/id_rsa.pub
```

**Примеры в проекте**:
- Файлы с учетными данными создаются с правами 644 для демонстрации

---

## 8. Встроенные уязвимости ОС

### 8.1. Уязвимости по умолчанию в Linux

**Критичность**: 🔴 КРИТИЧНО

**Описание**: Операционные системы часто поставляются с небезопасными настройками по умолчанию.

#### Ubuntu/Debian:

1. **Открытый доступ к информации о системе**:
   ```bash
   # /proc и /sys могут раскрывать информацию
   # Отключение ненужных модулей ядра
   ```

2. **Слабая конфигурация AppArmor/SELinux**:
   ```bash
   # Ubuntu использует AppArmor, но не все сервисы защищены
   # Необходимо настраивать профили для критичных приложений
   ```

3. **Открытые сетевые сервисы**:
   - Avahi (mDNS)
   - CUPS (печать)
   - Необходимо отключать неиспользуемые сервисы

#### CentOS/RHEL:

1. **SELinux в режиме permissive**:
   ```bash
   # По умолчанию может быть permissive
   # Необходимо установить enforcing
   setenforce 1
   sed -i 's/SELINUX=permissive/SELINUX=enforcing/' /etc/selinux/config
   ```

2. **Открытые порты**:
   - firewalld может быть не настроен
   - Необходимо настраивать правила

---

### 8.2. Критические уязвимости, требующие немедленного исправления

#### CVE-2021-44228 (Log4Shell)

**Описание**: Удаленное выполнение кода в Apache Log4j через JNDI lookup.

**Затронутые версии**: Log4j 2.0-beta9 до 2.14.1

**Исправление**:
```bash
# Обновление до версии 2.15.0 или выше
# Или установка системного свойства
-Dlog4j2.formatMsgNoLookups=true

# Временное исправление
export LOG4J_FORMAT_MSG_NO_LOOKUPS=true
```

**Примеры в проекте**: Используется для демонстрации в веб-сервере

---

#### CVE-2017-5638 (Apache Struts)

**Описание**: Удаленное выполнение кода через OGNL выражения.

**Затронутые версии**: Struts 2.3.x, 2.5.x

**Исправление**: Обновление до версии 2.5.10.1 или выше

---

#### CVE-2014-0160 (Heartbleed)

**Описание**: Утечка памяти в OpenSSL через расширение Heartbeat.

**Затронутые версии**: OpenSSL 1.0.1 - 1.0.1f

**Исправление**: Обновление OpenSSL до версии 1.0.1g или выше

---

### 8.3. Уязвимости конфигурации по умолчанию

#### SSH конфигурация

**Проблемы по умолчанию**:
- Разрешен root login
- Разрешена парольная аутентификация
- Слабые алгоритмы шифрования

**Исправление**:
```bash
# /etc/ssh/sshd_config
PermitRootLogin no
PasswordAuthentication no
PubkeyAuthentication yes

# Отключение слабых алгоритмов
KexAlgorithms curve25519-sha256@libssh.org,diffie-hellman-group-exchange-sha256
Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com
MACs hmac-sha2-256-etm@openssh.com,hmac-sha2-512-etm@openssh.com
```

---

#### MySQL/MariaDB конфигурация

**Проблемы по умолчанию**:
- Слабый пароль root или его отсутствие
- Разрешен удаленный доступ
- Отсутствие шифрования

**Исправление**:
```bash
# Запуск mysql_secure_installation
mysql_secure_installation

# Настройка /etc/mysql/mysql.conf.d/mysqld.cnf
bind-address = 127.0.0.1
require_secure_transport = ON
```

---

## Чек-лист безопасности

### При развертывании новой системы:

- [ ] Изменены все пароли по умолчанию
- [ ] Отключен root login по SSH
- [ ] Настроена аутентификация по ключам
- [ ] Настроен брандмауэр (только необходимые порты)
- [ ] Отключены неиспользуемые сервисы
- [ ] Включены автоматические обновления безопасности
- [ ] Настроено логирование критичных событий
- [ ] Настроен мониторинг безопасности
- [ ] Проверены права доступа к файлам
- [ ] Удалены учетные данные из конфигураций
- [ ] Настроена сегментация сети
- [ ] Используются только безопасные протоколы
- [ ] Проведен аудит безопасности

---

## Инструменты для проверки

### Сканеры уязвимостей:

1. **OpenVAS** — комплексное сканирование уязвимостей
2. **Nessus** — коммерческий сканер
3. **Nmap** — сканирование портов и сервисов
4. **Lynis** — аудит безопасности Linux систем
5. **OpenSCAP** — проверка соответствия стандартам безопасности

### Аудит конфигураций:

1. **CIS-CAT** — проверка соответствия CIS Benchmarks
2. **Ansible-lint** — проверка Ansible конфигураций
3. **Terraform validate** — валидация Terraform конфигураций

---

## Заключение

Большинство ошибок безопасности возникают из-за:
1. **Незнания** — администраторы не знают о рисках
2. **Удобства** — выбор простых решений вместо безопасных
3. **Времени** — отсутствие времени на правильную настройку
4. **Отсутствия политик** — нет четких требований безопасности

**Рекомендации**:
- Регулярное обучение администраторов
- Использование чек-листов безопасности
- Автоматизация проверок конфигураций
- Внедрение принципа минимальных привилегий
- Регулярный аудит безопасности

---

## Дополнительные ресурсы

- [CIS Benchmarks](https://www.cisecurity.org/cis-benchmarks/)
- [NIST Cybersecurity Framework](https://www.nist.gov/cyberframework)
- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [CVE Database](https://cve.mitre.org/)
- [Security Advisories](https://www.us-cert.gov/ncas/alerts)


