# Пример конфигурации для Terraform с Proxmox
# Скопируйте этот файл в terraform.tfvars и заполните значения

# URL API Proxmox
proxmox_api_url = "https://pve.example.com:8006/api2/json"

# Proxmox API Token (создается в Proxmox: Datacenter -> Permissions -> API Tokens)
proxmox_api_token_id     = "terraform@pam!terraform-token"
proxmox_api_token_secret = "your-secret-token-here"

# Имя узла Proxmox
proxmox_node = "pve"

# Имя шаблона Ubuntu Server в Proxmox
# Шаблон должен быть создан заранее с cloud-init
template_name = "ubuntu-20.04-template"

# Имя хранилища данных
storage = "local-lvm"

# SSH публичный ключ для Ubuntu Server
ssh_public_key = "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAABAQ..."

