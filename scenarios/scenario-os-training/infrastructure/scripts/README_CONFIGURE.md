# Интерактивный скрипт настройки уязвимостей

## Обзор

Скрипт `configure-vulnerabilities.sh` предоставляет интерактивное меню для выбора категорий уязвимостей, которые будут настроены на машинах учебной инфраструктуры.

## Использование

```bash
cd infrastructure/scripts
chmod +x configure-vulnerabilities.sh
./configure-vulnerabilities.sh
```

**📖 Подробный пример использования:** См. [EXAMPLE_CONFIGURE.md](EXAMPLE_CONFIGURE.md)

## Возможности

### 1. Настройка уязвимостей по категориям

**Ubuntu Server (12 категорий):**
- Выбор категорий через интерактивное меню
- Просмотр описания каждой категории (команда `d1`, `d2`, и т.д.)
- Визуальная индикация включенных/отключенных категорий

**Windows Server 2016 (10 категорий):**
- Аналогичный интерфейс для Windows Server
- Выбор категорий уязвимостей

**Windows 10 Pro (8 уязвимостей):**
- Выбор отдельных уязвимостей клиентской системы

### 2. Предустановленные профили

- **Начинающий:** Базовые уязвимости (Ubuntu: 1-4, Windows Server: 1-2, Windows Client: 1-4)
- **Средний уровень:** Расширенный набор (Ubuntu: 1-8, Windows Server: 1-6, Windows Client: все)
- **Продвинутый:** Все уязвимости включены

### 3. Просмотр и предпросмотр

- Просмотр текущей конфигурации
- Предпросмотр перед сохранением
- Подсчет выбранных категорий

### 4. Сохранение конфигурации

- Автоматическое создание файла `group_vars/all/vulnerabilities.yml`
- Готовый к использованию формат для Ansible
- Инструкции по применению конфигурации

## Пример использования

```
========================================
  Настройка уязвимостей учебной инфраструктуры
========================================

Выберите действие:

  1) Настроить уязвимости Ubuntu Server
  2) Настроить уязвимости Windows Server 2016
  3) Настроить уязвимости Windows 10 Pro
  4) Предустановленные профили (Начинающий/Средний/Продвинутый)
  5) Просмотр текущей конфигурации
  6) Предпросмотр конфигурации
  7) Сохранить конфигурацию
  8) Выход

Ваш выбор: 1

=== Настройка уязвимостей Ubuntu Server ===

Категории уязвимостей Ubuntu Server:

  1)  Аутентификация и управление аккаунтами
  2)  Уязвимости SSH
  3)  Ошибки конфигурации сети и firewall
  ...

Текущий статус:
  Категория 1: ✅
  Категория 2: ✅
  Категория 3: ❌
  ...

Команды:
  • Введите номер категории (1-12) для включения/отключения
  • Введите 'd' + номер для просмотра описания
  • Введите 0 для возврата в главное меню

Ваш выбор: d1

=== Описание категории 1 ===

1. ОШИБКИ В УПРАВЛЕНИИ АККАУНТАМИ И АУТЕНТИФИКАЦИЕЙ

Что будет настроено:
  • Создание пользователей со слабыми паролями:
    - admin/admin123
    - user1/password123
    - user2/123456
    - root/root123
  • Отключение PAM-ограничений (minlen = 1)
  • Отключение блокировки после неверных попыток
  • Пароли никогда не истекают (chage -M -1)
  • Широкие sudo права (NOPASSWD для всех)
  • Отсутствие MFA

Риски: Легкий брутфорс паролей, компрометация аккаунтов
```

## Структура меню

### Главное меню
- Настройка уязвимостей для каждой ОС
- Предустановленные профили
- Просмотр и предпросмотр
- Сохранение

### Меню категорий
- Список всех категорий с индикацией статуса
- Выбор категории для включения/отключения
- Просмотр описания (команда `d` + номер)

## Сохранение конфигурации

После выбора категорий конфигурация сохраняется в:
```
infrastructure/ansible/group_vars/all/vulnerabilities.yml
```

Файл готов к использованию с Ansible playbook.

## Применение конфигурации

После сохранения:

```bash
cd infrastructure/ansible
ansible-playbook -i inventory.yml playbook.yml
```

**⚠️ ВАЖНО:** Playbook должен быть обновлен для использования переменных из `vulnerabilities.yml` (см. `PLAYBOOK_UPDATE_GUIDE.md`).

## Примеры конфигураций

### Начинающий уровень

```yaml
ubuntu_vulnerabilities:
  auth_weak_passwords: true
  ssh_password_auth: true
  network_firewall_off: true
  updates_disabled: true
  # Остальные: false
```

### Продвинутый уровень

```yaml
ubuntu_vulnerabilities:
  # Все установлено в true
  auth_weak_passwords: true
  ssh_password_auth: true
  # ... все остальные true
```

## Документация

- **Пример использования:** [EXAMPLE_CONFIGURE.md](EXAMPLE_CONFIGURE.md) - пошаговый пример настройки
- Полное описание уязвимостей: `../../docs/overview/machine-scenarios.md`
- Руководство по обновлению playbook: `../ansible/PLAYBOOK_UPDATE_GUIDE.md`
- Примеры конфигураций: `../ansible/group_vars/all/vulnerabilities.example.yml`

