# Методические указания для студентов

## 📚 Общая информация

**Название практики:** Защита операционных систем  
**Длительность:** 2-4 академических часа  
**Уровень сложности:** Начинающий / Средний / Продвинутый  
**Предварительные знания:** Базовые знания Linux и Windows, основы сетевой безопасности

---

## 🎯 Цели практики

После выполнения практики вы должны:

1. **Уметь обнаруживать** типичные уязвимости в конфигурации ОС
2. **Понимать риски** неправильной настройки систем
3. **Уметь исправлять** найденные уязвимости
4. **Применять** лучшие практики безопасности

---

## 📋 Структура практики

### Этап 1: Подготовка (15-20 минут)

#### 1.1 Ознакомление с инфраструктурой

**Задача:** Изучите состав учебной инфраструктуры

**Действия:**
1. Прочитайте [обзор сценария](../../overview/README.md)
2. Изучите описание машин и уязвимостей: [machine-scenarios.md](../../overview/machine-scenarios.md)
3. Ознакомьтесь с сетевым окружением:
   - Ubuntu Server: `192.168.100.10`
   - Windows Server 2016: `192.168.100.20`
   - Windows 10 Pro: `192.168.100.30`
   - Все машины в сети `192.168.100.0/24`

**Проверка:** Убедитесь, что вы понимаете:
- Какие машины входят в инфраструктуру
- Какие уязвимости должны быть на каждой машине
- Как машины связаны между собой

#### 1.2 Подключение к машинам

**Задача:** Установите соединение со всеми машинами

**Для Ubuntu Server:**
```bash
ssh ubuntu@192.168.100.10
# Пароль: ubuntu123
```

**Для Windows Server 2016:**
- RDP: `mstsc /v:192.168.100.20`
- Учетные данные: `Administrator / Admin123!`

**Для Windows 10 Pro:**
- RDP: `mstsc /v:192.168.100.30`
- Учетные данные: `user / password123`

**Проверка:** Убедитесь, что можете подключиться ко всем трем машинам.

---

### Этап 2: Обнаружение уязвимостей (60-90 минут)

#### 2.1 Ubuntu Server - Обнаружение уязвимостей

**Категория 1: Ошибки в управлении аккаунтами и аутентификацией**

**Задача:** Обнаружить слабые пароли и неправильные настройки аутентификации

**Действия:**
1. Проверьте файл `/etc/passwd`:
   ```bash
   cat /etc/passwd
   ```
   - Найдите пользователей со слабыми паролями
   - Проверьте наличие пользователя `root` с паролем

2. Проверьте настройки PAM:
   ```bash
   cat /etc/pam.d/common-password
   ```
   - Найдите параметр `minlen` (минимальная длина пароля)
   - Проверьте наличие ограничений на сложность

3. Проверьте настройки sudo:
   ```bash
   sudo cat /etc/sudoers
   ```
   - Найдите правила с `NOPASSWD`
   - Проверьте, какие пользователи имеют sudo права

**Ожидаемые находки:**
- Пользователи с паролями: `admin/admin123`, `user1/password123`
- `minlen = 1` в PAM (нет ограничений на длину пароля)
- Широкие sudo права без пароля

**Документирование:** Запишите все найденные уязвимости в отчет.

---

**Категория 2: Уязвимости при конфигурации SSH**

**Задача:** Обнаружить небезопасные настройки SSH

**Действия:**
1. Проверьте конфигурацию SSH:
   ```bash
   sudo cat /etc/ssh/sshd_config | grep -E "PermitRootLogin|PasswordAuthentication|MaxAuthTries"
   ```

2. Проверьте разрешенные алгоритмы:
   ```bash
   sudo sshd -T | grep -E "ciphers|macs|kexalgorithms"
   ```

3. Проверьте наличие fail2ban:
   ```bash
   systemctl status fail2ban
   ```

**Ожидаемые находки:**
- `PermitRootLogin yes` (разрешен вход root)
- `PasswordAuthentication yes` (парольная аутентификация)
- `MaxAuthTries 1000` (неограниченные попытки)
- Слабые алгоритмы шифрования
- fail2ban не установлен или не запущен

---

**Категория 3: Ошибки конфигурации сети и firewall**

**Задача:** Обнаружить проблемы с сетевым экраном

**Действия:**
1. Проверьте статус UFW:
   ```bash
   sudo ufw status
   ```

2. Проверьте открытые порты:
   ```bash
   sudo netstat -tulpn
   # или
   sudo ss -tulpn
   ```

3. Проверьте наличие небезопасных служб:
   ```bash
   sudo systemctl list-units --type=service --state=running | grep -E "telnet|ftp|rlogin"
   ```

**Ожидаемые находки:**
- UFW отключен
- Открыты порты: 23 (Telnet), 21 (FTP), 513 (Rlogin)
- Небезопасные службы запущены

---

**Категория 6: Ошибки с правами доступа и файлами**

**Задача:** Обнаружить неправильные права доступа

**Действия:**
1. Проверьте права на системные директории:
   ```bash
   ls -ld /etc /var /home
   ```

2. Проверьте права на `/etc/shadow`:
   ```bash
   ls -l /etc/shadow
   ```

3. Найдите директории с правами 777:
   ```bash
   find / -type d -perm 777 2>/dev/null
   ```

**Ожидаемые находки:**
- `/etc` с правами `777`
- `/var` с правами `777`
- `/etc/shadow` с неправильными правами (не `640`)

---

#### 2.2 Windows Server 2016 - Обнаружение уязвимостей

**Категория 1: Уязвимости аутентификации**

**Задача:** Обнаружить слабые пароли и неправильные политики

**Действия:**
1. Откройте Local Security Policy:
   - `Win + R` → `secpol.msc`

2. Проверьте политику паролей:
   - Account Policies → Password Policy
   - Проверьте: Minimum password length, Password complexity, Password history

3. Проверьте учетные записи:
   - Computer Management → Local Users and Groups → Users
   - Найдите пользователей со слабыми паролями

**Ожидаемые находки:**
- Минимальная длина пароля: 1 символ
- Политика сложности отключена
- Пароли никогда не истекают
- Пользователи: `admin/admin123`, `user1/password123`

---

**Категория 4: Уязвимости конфигурации сети**

**Задача:** Обнаружить небезопасные сетевые протоколы

**Действия:**
1. Проверьте статус SMBv1:
   ```powershell
   Get-WindowsOptionalFeature -Online -FeatureName SMB1Protocol
   ```

2. Проверьте настройки NTLM:
   ```powershell
   Get-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Lsa" -Name "LmCompatibilityLevel"
   ```

3. Проверьте правила брандмауэра:
   ```powershell
   Get-NetFirewallRule | Where-Object {$_.Enabled -eq $true} | Select-Object DisplayName, Direction, Action
   ```

**Ожидаемые находки:**
- SMBv1 включен
- NTLMv1 разрешен
- Широкие правила брандмауэра

---

#### 2.3 Windows 10 Pro - Обнаружение уязвимостей

**Задача:** Обнаружить уязвимости клиентской системы

**Действия:**
1. Проверьте статус UAC:
   ```powershell
   Get-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System" -Name "EnableLUA"
   ```

2. Проверьте статус Windows Defender:
   ```powershell
   Get-MpComputerStatus
   ```

3. Проверьте статус брандмауэра:
   ```powershell
   Get-NetFirewallProfile | Select-Object Name, Enabled
   ```

**Ожидаемые находки:**
- UAC отключен
- Windows Defender отключен
- Брандмауэр отключен

---

### Этап 3: Исправление уязвимостей (60-90 минут)

#### 3.1 Ubuntu Server - Исправление уязвимостей

**Категория 1: Исправление аутентификации**

**Задача:** Настроить безопасную аутентификацию

**Действия:**
1. Настройте PAM для минимальной длины пароля 12 символов:
   ```bash
   sudo nano /etc/pam.d/common-password
   # Измените: password requisite pam_pwquality.so minlen=12
   ```

2. Отключите root login:
   ```bash
   sudo nano /etc/ssh/sshd_config
   # Установите: PermitRootLogin no
   sudo systemctl restart sshd
   ```

3. Настройте sudo права:
   ```bash
   sudo visudo
   # Удалите правила с NOPASSWD
   # Ограничьте sudo права только для необходимых пользователей
   ```

4. Установите и настройте fail2ban:
   ```bash
   sudo apt update
   sudo apt install -y fail2ban
   sudo systemctl enable fail2ban
   sudo systemctl start fail2ban
   ```

**Проверка:**
```bash
# Проверьте настройки PAM
grep minlen /etc/pam.d/common-password

# Проверьте SSH конфигурацию
sudo sshd -T | grep permitrootlogin

# Проверьте fail2ban
sudo systemctl status fail2ban
```

---

**Категория 2: Исправление SSH**

**Задача:** Настроить безопасный SSH

**Действия:**
1. Отключите парольную аутентификацию (используйте только ключи):
   ```bash
   sudo nano /etc/ssh/sshd_config
   # Установите:
   # PasswordAuthentication no
   # PermitRootLogin no
   # MaxAuthTries 3
   ```

2. Отключите слабые алгоритмы:
   ```bash
   # Добавьте в /etc/ssh/sshd_config:
   KexAlgorithms curve25519-sha256@libssh.org,diffie-hellman-group-exchange-sha256
   Ciphers chacha20-poly1305@openssh.com,aes256-gcm@openssh.com,aes128-gcm@openssh.com
   MACs hmac-sha2-256-etm@openssh.com,hmac-sha2-512-etm@openssh.com
   ```

3. Перезапустите SSH:
   ```bash
   sudo systemctl restart sshd
   ```

**Проверка:**
```bash
sudo sshd -T | grep -E "passwordauthentication|permitrootlogin|maxauthtries"
```

---

**Категория 3: Исправление firewall**

**Задача:** Настроить UFW

**Действия:**
1. Включите UFW:
   ```bash
   sudo ufw enable
   ```

2. Разрешите только необходимые порты:
   ```bash
   sudo ufw allow 22/tcp  # SSH
   sudo ufw allow 80/tcp   # HTTP (если нужен)
   sudo ufw allow 443/tcp  # HTTPS (если нужен)
   ```

3. Отключите небезопасные службы:
   ```bash
   sudo systemctl stop telnet
   sudo systemctl disable telnet
   sudo systemctl stop ftp
   sudo systemctl disable ftp
   ```

**Проверка:**
```bash
sudo ufw status verbose
sudo netstat -tulpn | grep -E "23|21|513"
```

---

**Категория 6: Исправление прав доступа**

**Задача:** Установить правильные права доступа

**Действия:**
1. Исправьте права на системные директории:
   ```bash
   sudo chmod 755 /etc
   sudo chmod 755 /var
   sudo chmod 755 /home
   ```

2. Исправьте права на `/etc/shadow`:
   ```bash
   sudo chmod 640 /etc/shadow
   sudo chown root:shadow /etc/shadow
   ```

3. Найдите и исправьте директории с правами 777:
   ```bash
   find / -type d -perm 777 2>/dev/null | while read dir; do
     sudo chmod 755 "$dir"
   done
   ```

**Проверка:**
```bash
ls -ld /etc /var /home
ls -l /etc/shadow
find / -type d -perm 777 2>/dev/null
```

---

#### 3.2 Windows Server 2016 - Исправление уязвимостей

**Категория 1: Исправление политики паролей**

**Задача:** Настроить безопасную политику паролей

**Действия:**
1. Откройте Local Security Policy: `secpol.msc`

2. Настройте Password Policy:
   - Minimum password length: 12
   - Password must meet complexity requirements: Enabled
   - Enforce password history: 24 passwords remembered
   - Maximum password age: 90 days
   - Minimum password age: 1 day

3. Измените пароли пользователей на сложные:
   ```powershell
   # Для каждого пользователя
   net user admin "NewComplexPassword123!"
   ```

**Проверка:**
```powershell
Get-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Services\Netlogon\Parameters" | Select-Object MinimumPasswordLength
```

---

**Категория 4: Отключение SMBv1 и NTLMv1**

**Задача:** Отключить устаревшие протоколы

**Действия:**
1. Отключите SMBv1:
   ```powershell
   Disable-WindowsOptionalFeature -Online -FeatureName SMB1Protocol -Remove
   ```

2. Отключите NTLMv1:
   ```powershell
   Set-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Lsa" -Name "LmCompatibilityLevel" -Value 5
   ```

3. Настройте брандмауэр:
   ```powershell
   # Разрешите только необходимые порты
   New-NetFirewallRule -DisplayName "Allow SMB" -Direction Inbound -Protocol TCP -LocalPort 445 -Action Allow
   ```

**Проверка:**
```powershell
Get-WindowsOptionalFeature -Online -FeatureName SMB1Protocol
Get-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\Lsa" -Name "LmCompatibilityLevel"
```

---

#### 3.3 Windows 10 Pro - Исправление уязвимостей

**Задача:** Включить защитные механизмы

**Действия:**
1. Включите UAC:
   ```powershell
   Set-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System" -Name "EnableLUA" -Value 1
   ```

2. Включите Windows Defender:
   ```powershell
   Set-MpPreference -DisableRealtimeMonitoring $false
   Start-MpScan
   ```

3. Включите брандмауэр:
   ```powershell
   Set-NetFirewallProfile -Profile Domain,Public,Private -Enabled True
   ```

**Проверка:**
```powershell
Get-ItemProperty -Path "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System" -Name "EnableLUA"
Get-MpComputerStatus
Get-NetFirewallProfile | Select-Object Name, Enabled
```

---

### Этап 4: Проверка исправлений (20-30 минут)

**Задача:** Убедиться, что все уязвимости исправлены

**Действия:**
1. Используйте автоматизированную проверку:
   ```bash
   cd infrastructure/scripts
   ./verify-fixes.sh
   ```

2. Или используйте Ansible playbook:
   ```bash
   cd infrastructure/ansible
   ansible-playbook -i inventory.yml verify-fixes.yml
   ```

3. Выполните ручную проверку по [чек-листу](../../assessment/CHECKLIST.md)

**Ожидаемый результат:** Все проверки должны пройти успешно (✓)

---

## 📝 Отчет о выполненной работе

### Структура отчета

1. **Титульный лист**
   - ФИО студента
   - Группа
   - Дата выполнения

2. **Введение**
   - Цель практики
   - Описание инфраструктуры

3. **Обнаруженные уязвимости**
   - Для каждой машины:
     - Категория уязвимости
     - Описание проблемы
     - Команды для обнаружения
     - Скриншоты/вывод команд

4. **Исправление уязвимостей**
   - Для каждой уязвимости:
     - Описание исправления
     - Выполненные команды
     - Результат проверки

5. **Заключение**
   - Количество найденных уязвимостей
   - Количество исправленных уязвимостей
   - Выводы и рекомендации

### Критерии оценки

- **Обнаружение уязвимостей:** 40%
  - Полнота обнаружения
  - Правильность описания
  - Документирование

- **Исправление уязвимостей:** 40%
  - Корректность исправлений
  - Применение лучших практик
  - Проверка результата

- **Отчет:** 20%
  - Структура и оформление
  - Полнота информации
  - Выводы и анализ

---

## 🛠️ Полезные инструменты

### Для Ubuntu Server
- `nmap` - сканирование портов
- `fail2ban` - защита от брутфорса
- `ufw` - управление firewall
- `chmod`, `chown` - управление правами
- `auditd` - аудит системы

### Для Windows
- `PowerShell` - автоматизация
- `Local Security Policy` - политики безопасности
- `Windows Defender` - антивирус
- `Windows Firewall` - сетевой экран
- `Event Viewer` - просмотр логов

---

## ❓ Часто задаваемые вопросы

**Q: Что делать, если не могу подключиться к машине?**  
A: Проверьте сетевую связность: `ping 192.168.100.10`. Убедитесь, что машины запущены.

**Q: Как проверить, что уязвимость исправлена?**  
A: Используйте скрипт `verify-fixes.sh` или выполните проверку вручную по чек-листу.

**Q: Можно ли использовать другие инструменты?**  
A: Да, вы можете использовать любые инструменты, которые помогают обнаружить и исправить уязвимости.

**Q: Что делать, если не получается исправить уязвимость?**  
A: Опишите проблему в отчете, укажите, что было сделано, и какие ошибки возникли.

---

## 📚 Дополнительные ресурсы

- [Полное описание уязвимостей](../../overview/machine-scenarios.md)
- [Чек-лист проверки](../../assessment/CHECKLIST.md)
- [Методы тестирования](../../assessment/TESTING.md)
- [Цели обучения](objectives.md)

---

## ⚠️ Важные замечания

1. **Не используйте эти настройки в продакшене!** Это учебная инфраструктура с намеренно настроенными уязвимостями.

2. **Документируйте все действия** - это поможет при написании отчета.

3. **Делайте скриншоты** - они пригодятся для отчета.

4. **Проверяйте результат** после каждого исправления.

5. **Используйте автоматизированные проверки** для экономии времени.

---

**Удачи в выполнении практики! 🚀**

