# Валидация развертывания инфраструктуры

> 📚 **Навигация**: См. [../README.md](../README.md) для полной навигации по документации.

## Обзор процесса

Данный документ описывает полный процесс проверки развертывания учебной инфраструктуры.

## Этап 1: Предварительная проверка

### 1.1. Проверка системных требований

```bash
# Проверка RAM
free -h
# Должно быть минимум 16 GB свободно

# Проверка дискового пространства
df -h
# Должно быть минимум 200 GB свободно

# Проверка CPU
nproc
# Должно быть минимум 4 ядра
```

### 1.2. Проверка установленного ПО

```bash
# Terraform
terraform version
# Должен быть >= 1.0

# Ansible
ansible --version
# Должен быть >= 2.9

# libvirt
virsh version
# Должен быть установлен

# qemu-kvm
qemu-system-x86_64 --version
# Должен быть установлен
```

### 1.3. Проверка образов ОС

```bash
# Ubuntu Cloud Image
ls -lh /var/lib/libvirt/images/focal-server-cloudimg-amd64.img
# Должен существовать и быть ~600 MB

# Windows ISO (опционально, для ручной установки)
ls -lh /var/lib/libvirt/images/Win2016.iso
ls -lh /var/lib/libvirt/images/Win10.iso
```

### 1.4. Проверка сети libvirt

```bash
# Проверка сетей
sudo virsh net-list --all
# Должна быть сеть "default" в статусе "active"

# Если сеть не активна
sudo virsh net-start default
sudo virsh net-autostart default
```

### 1.5. Проверка SSH ключей

```bash
# Проверка наличия SSH ключа
ls -la ~/.ssh/id_rsa.pub ~/.ssh/id_ed25519.pub
# Должен существовать хотя бы один

# Просмотр публичного ключа
cat ~/.ssh/id_ed25519.pub || cat ~/.ssh/id_rsa.pub
```

## Этап 2: Проверка Terraform конфигурации

### 2.1. Структура файлов

```bash
cd scenarios/scenario-os-training/infrastructure/terraform
ls -la
```

Должны присутствовать:
- `main.tf` - основная конфигурация
- `README.md` - документация

### 2.2. Валидация конфигурации

```bash
# Инициализация
terraform init
# Должно завершиться без ошибок

# Валидация синтаксиса
terraform validate
# Должно вернуть: "Success! The configuration is valid."

# Проверка форматирования
terraform fmt -check
# Должно вернуть без ошибок
```

### 2.3. Проверка плана

```bash
# Установка переменных
export TF_VAR_ssh_public_key="$(cat ~/.ssh/id_ed25519.pub || cat ~/.ssh/id_rsa.pub)"
export TF_VAR_base_image_path="/var/lib/libvirt/images/focal-server-cloudimg-amd64.img"

# План развертывания
terraform plan
```

Ожидаемый результат:
- Планируется создание 1 ресурса (libvirt_domain)
- Планируется создание связанных ресурсов (диски, cloud-init)
- Нет ошибок

## Этап 3: Развертывание через Terraform

### 3.1. Применение конфигурации

```bash
terraform apply
# Или с автоматическим подтверждением:
terraform apply -auto-approve
```

### 3.2. Проверка созданных ресурсов

```bash
# Проверка VM через virsh
sudo virsh list --all
# Должна быть VM "ubuntu-server-training-1" в статусе "running"

# Получение IP адреса
terraform output ubuntu_server_ip
# Должен вернуть IP: 192.168.100.10
```

### 3.3. Проверка сетевого подключения

```bash
# Ожидание инициализации (30-60 секунд)
sleep 60

# Проверка доступности
ping -c 3 192.168.100.10
# Должен отвечать на ping

# Проверка SSH
ssh -o StrictHostKeyChecking=no ubuntu@192.168.100.10 "echo 'SSH работает'"
# Должен вернуть: "SSH работает"
```

## Этап 4: Проверка Ansible конфигурации

### 4.1. Проверка inventory

```bash
cd ../ansible

# Просмотр inventory
ansible-inventory -i inventory.yml --list
```

Ожидаемый результат:
- Группа `linux` с хостом `ubuntu-server` (192.168.100.10)
- Группа `windows` с хостами:
  - `windows-server-2016` (192.168.100.20)
  - `windows-10-pro` (192.168.100.30)

### 4.2. Проверка синтаксиса playbook

```bash
# Проверка синтаксиса
ansible-playbook --syntax-check -i inventory.yml playbook.yml
# Должно вернуть: "playbook: playbook.yml" без ошибок
```

### 4.3. Проверка подключения

```bash
# Проверка подключения к Ubuntu Server
ansible ubuntu-server -i inventory.yml -m ping
# Должно вернуть: "pong"

# Проверка всех доступных хостов
ansible all -i inventory.yml -m ping
# Ubuntu Server должен отвечать
# Windows машины могут не отвечать (если не установлены)
```

## Этап 5: Настройка уязвимостей через Ansible

### 5.1. Выполнение playbook для Ubuntu

```bash
# Выполнение playbook
ansible-playbook -i inventory.yml playbook.yml --limit ubuntu-server
```

Ожидаемый результат:
- Все задачи выполнены или пропущены с ожидаемыми причинами
- Нет критических ошибок
- Handlers выполнены (restart ssh, restart samba, и т.д.)

### 5.2. Проверка уязвимостей на Ubuntu Server

Подключитесь к серверу и проверьте каждую категорию:

```bash
ssh ubuntu@192.168.100.10
```

#### Категория 1: Управление аккаунтами
```bash
# Проверка PAM-ограничений
grep minlen /etc/security/pwquality.conf
# Должно быть: minlen = 1

# Проверка пользователей
cat /etc/passwd | grep -E "admin|user1|user2"
# Должны существовать пользователи

# Проверка sudo прав
sudo cat /etc/sudoers.d/vulnerable
# Должен содержать NOPASSWD: ALL
```

#### Категория 2: SSH конфигурация
```bash
# Проверка SSH настроек
grep -E "PermitRootLogin|PasswordAuthentication|MaxAuthTries" /etc/ssh/sshd_config
# Должно быть: PermitRootLogin yes, PasswordAuthentication yes, MaxAuthTries 1000

# Проверка fail2ban
systemctl status fail2ban
# Должен быть stopped
```

#### Категория 3: Firewall
```bash
# Проверка UFW
ufw status
# Должно быть: Status: inactive

# Проверка iptables
sudo iptables -L
# Должны быть разрешающие правила
```

#### Категория 4: Обновления
```bash
# Проверка автоматических обновлений
ls /etc/apt/apt.conf.d/20auto-upgrades
# Файл должен отсутствовать
```

#### Категория 5: Права доступа
```bash
# Проверка прав на системные директории
ls -ld /etc /var /home
# Должны быть права 777 (drwxrwxrwx)
```

#### Категория 6-12: Остальные уязвимости
```bash
# Проверка информационного файла
cat /root/VULNERABILITY_INFO.txt
# Должен содержать описание всех 12 категорий уязвимостей
```

## Этап 6: Проверка Windows машин (если установлены)

### 6.1. Проверка подключения

```powershell
# С хоста Linux (если установлен PowerShell)
Test-NetConnection -ComputerName 192.168.100.20 -Port 5985
# Или через telnet
telnet 192.168.100.20 5985
```

### 6.2. Проверка через Ansible

```bash
# Проверка подключения
ansible windows-server-2016 -i inventory.yml -m win_ping
# Должно вернуть: "pong"

# Выполнение playbook для Windows
ansible-playbook -i inventory.yml playbook.yml --limit windows
```

## Этап 7: Итоговая проверка

### 7.1. Проверка всех машин

```bash
# Проверка доступности всех машин
ansible all -i inventory.yml -m ping

# Сбор информации о системах
ansible all -i inventory.yml -m setup
```

### 7.2. Проверка сетевой связности

```bash
# С Ubuntu Server
ssh ubuntu@192.168.100.10

# Проверка связи с Windows (если установлены)
ping -c 3 192.168.100.20
ping -c 3 192.168.100.30
```

### 7.3. Проверка документации

- [ ] DEPLOYMENT.md прочитан
- [ ] QUICKSTART.md прочитан
- [ ] CHECKLIST.md пройден
- [ ] Все шаги выполнены успешно

## Критерии успешного развертывания

✅ **Успешно**, если:
1. Terraform создал Ubuntu Server без ошибок
2. Ubuntu Server доступен по SSH (192.168.100.10)
3. Ansible playbook выполнен для Ubuntu Server
4. Все 12 категорий уязвимостей настроены
5. Информационный файл `/root/VULNERABILITY_INFO.txt` создан
6. Windows машины установлены и настроены (опционально)

❌ **Требует исправления**, если:
1. Terraform не может создать VM
2. Ubuntu Server не получает IP адрес
3. SSH подключение не работает
4. Ansible playbook завершается с ошибками
5. Уязвимости не настроены

## Следующие шаги

После успешной валидации:
1. Начните обучение с обнаружения уязвимостей
2. Используйте материалы из `../overview/machine-scenarios.md`
3. Выполняйте задания по исправлению уязвимостей
4. Документируйте результаты в отчете

