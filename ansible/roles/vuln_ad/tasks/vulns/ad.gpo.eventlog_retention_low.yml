---
- name: Reduce Event Log size/retention via GPO (OU Training) (best-effort)
  ansible.windows.win_shell: |
    $gpoName = "{{ ad_gpo_prefix | default('training-ad.gpo.') }}eventlog_retention_low"
    $ouName = "{{ ad_training_ou | default('Training') }}"
    try {
      Import-Module ActiveDirectory -ErrorAction Stop
      Import-Module GroupPolicy -ErrorAction Stop
      $dn = (Get-ADDomain).DistinguishedName
      $ouDn = "OU=$ouName,$dn"
      if (-not (Get-ADOrganizationalUnit -LDAPFilter "(distinguishedName=$ouDn)" -ErrorAction SilentlyContinue)) {
        New-ADOrganizationalUnit -Name $ouName -Path $dn -ProtectedFromAccidentalDeletion $false | Out-Null
      }
      if (-not (Get-GPO -Name $gpoName -ErrorAction SilentlyContinue)) {
        New-GPO -Name $gpoName | Out-Null
      }
      try { New-GPLink -Name $gpoName -Target $ouDn -LinkEnabled Yes -ErrorAction Stop | Out-Null } catch {}

      $logs = @("Application","System","Security","Windows PowerShell")
      foreach ($l in $logs) {
        $key = "HKLM\SOFTWARE\Policies\Microsoft\Windows\EventLog\$l"
        Set-GPRegistryValue -Name $gpoName -Key $key -ValueName "MaxSize" -Type DWord -Value 1024
        Set-GPRegistryValue -Name $gpoName -Key $key -ValueName "Retention" -Type String -Value "0"
      }
      "OK"
    } catch {
      "SKIP/ERR"
    }
  changed_when: false
  failed_when: false

