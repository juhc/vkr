- name: Create extra admin user (unnecessary privileged account)
  ansible.builtin.user:
    name: "{{ vuln_linux_extra_admin_user }}"
    groups: sudo
    append: true
    password: "{{ vuln_linux_weak_password | password_hash('sha512') }}"
    shell: /bin/bash
    create_home: true

