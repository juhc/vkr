- name: Allow passwordless sudo (NOPASSWD) for all sudo users
  ansible.builtin.copy:
    dest: /etc/sudoers.d/99-training-nopasswd
    mode: "0440"
    content: |
      %sudo ALL=(ALL) NOPASSWD:ALL
      {{ vuln_linux_extra_admin_user }} ALL=(ALL) NOPASSWD:ALL

