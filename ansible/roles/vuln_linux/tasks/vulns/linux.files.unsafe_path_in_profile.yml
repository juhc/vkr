- name: Add world-writable directory to PATH system-wide (insecure)
  ansible.builtin.copy:
    dest: /etc/profile.d/training-unsafe-path.sh
    mode: "0644"
    content: |
      # TRAINING VULN: insecure PATH (world-writable directory first)
      export PATH="/tmp:$PATH"

