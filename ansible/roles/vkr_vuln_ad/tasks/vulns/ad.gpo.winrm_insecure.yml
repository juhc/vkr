---
- name: Enable insecure WinRM settings via GPO (OU Training) (best-effort)
  ansible.windows.win_shell: |
    $gpoName = "VKR-ad.gpo.winrm_insecure"
    $ouName = "{{ vkr_ad_training_ou | default('Training') }}"
    try {
      Import-Module ActiveDirectory -ErrorAction Stop
      Import-Module GroupPolicy -ErrorAction Stop
      $dn = (Get-ADDomain).DistinguishedName
      $ouDn = "OU=$ouName,$dn"
      if (-not (Get-ADOrganizationalUnit -LDAPFilter "(distinguishedName=$ouDn)" -ErrorAction SilentlyContinue)) {
        New-ADOrganizationalUnit -Name $ouName -Path $dn -ProtectedFromAccidentalDeletion $false | Out-Null
      }
      if (-not (Get-GPO -Name $gpoName -ErrorAction SilentlyContinue)) {
        New-GPO -Name $gpoName | Out-Null
      }
      try { New-GPLink -Name $gpoName -Target $ouDn -LinkEnabled Yes -ErrorAction Stop | Out-Null } catch {}

      Set-GPRegistryValue -Name $gpoName -Key "HKLM\SOFTWARE\Policies\Microsoft\Windows\WinRM\Service" -ValueName "AllowUnencryptedTraffic" -Type DWord -Value 1
      Set-GPRegistryValue -Name $gpoName -Key "HKLM\SOFTWARE\Policies\Microsoft\Windows\WinRM\Service\Auth" -ValueName "Basic" -Type DWord -Value 1
      "OK"
    } catch {
      "SKIP/ERR"
    }
  changed_when: false
  failed_when: false

